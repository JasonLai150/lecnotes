# Math final-review fix wave — brief (ONE commit)

Repo: /Users/jasonlai150/Documents/GitHub/lecnotes. Math feature complete, 325 tests. The final review's decisions are in "Amendments (2026-09-15, from the final review)" at the end of `docs/superpowers/specs/2026-09-15-lecnotes-math-design.md`. Fix all items below test-first, then make ONE commit (the user wants few commits). Don't push. Don't commit docs/superpowers/sdd/.

1. **Unclosed `$$` swallows later content (mdparse.py ~57-62).** First confirm the installed mdit-py-plugins `dollarmath_plugin` accepts `allow_blank_lines` (`inspect.signature`). If it does, pass `allow_blank_lines=False`. If it does not, report NEEDS_CONTEXT with the signature — do not work around it. Tests (tests/test_mdparse.py and tests/test_figures_refs.py or test_finish.py):
   - `"$$\nx = 1\n\n## Heading\n\n![f](figures/slide-003.png)\n\nText.\n\n$$\ny\n$$\n"`: a `heading_open` token exists, `find_refs` returns `[3]`, and the final `$$\ny\n$$` is a `math_block`.
   - `"$$x = 1$$.\n\n## Heading\n\n![f](figures/slide-003.png)\n\n$$\ny\n$$\n"`: same assertions (heading present, `find_refs == [3]`).
2. **Pipes in table-cell math (templates/instructions.md).** In the Equations section add, as its own sentence: `Inside a table cell, write \mid, \vert, or \lVert … \rVert instead of | or \| — a bare pipe splits the cell.` (keep the backslash commands in backticks, matching the section's style: `` `\mid` ``, `` `\vert` ``, `` `\lVert … \rVert` ``, `` `|` ``, `` `\|` ``). Test in tests/test_instructions.py asserting `\mid` and "table cell" appear.
3. **List items and titles (templates/instructions.md).** Adjust the flush-left sentence so it reads: `Put each $$ on its own line, flush left with no indentation (the indentation above only marks the example) — except inside a list item, where the $$ lines are indented to line up with the item's text — with a blank line before and after the block.` (backticks around `$$` as now). Add a sentence: `Keep math out of the # title.` (backticks around `#`). Tests assert "inside a list item" and "out of the" + "title" appear. Add a parser test documenting it: `"1. Item text\n\n   $$\n   x^2\n   $$\n\n2. Next\n"` yields a `math_block` token and exactly one `ordered_list_open`.
4. **Stale "no JavaScript" wording.** README.md (~line 67) and the `src/lecnotes/export_html.py` module docstring: say there is no JavaScript unless the notes contain math (a bundled copy of KaTeX renders it) and nothing loads from the network.
5. **Image alt text loses math (export_html.py).** In the loop that rewrites local image `src`, also set the image's alt to `inline_text(child.children or [])` so math survives; confirm markdown-it-py's image renderer uses `token.attrs["alt"]` or rebuilds from children — if it rebuilds from children, add an `image` render rule that renders `alt` via `inline_text` instead. Test: `![loop $\pi_\theta$](f.png)` renders `alt="loop \pi_\theta"`.
6. **Doubled display margin (export_html.py `_MATH_CSS`).** Add `.lecnotes-math-display .katex-display { margin: 0; }`. Test asserts that rule is in math output.

Out of scope (backlog): rendering math inside figcaptions, blockquotes nested in list items at 4+ spaces of indentation.

Constraints: deps unchanged (pymupdf, markdown-it-py, mdit-py-plugins); leaf modules raise no LecnotesError; full suite green, no warnings. Commit message: subject, blank line, `Co-Authored-By: Claude <your model name> <noreply@anthropic.com>`, `Claude-Session: https://claude.ai/code/session_01YAyFL8DWShjsveMRqBQ4Dy` on their own lines. Also update docs/BACKLOG.md (committable): under Known issues add "Math in figure captions shows as LaTeX source rather than rendered" and "A blockquote nested in a list item at 4+ spaces of indentation still leaks `>` into display math"; under Not yet verified keep the Notion math item.

Before committing, do one visual check: export a small document with an aligned display block inside a list item, a table using `\mid`, and a figure with math in its alt, open it with `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome --headless=new --screenshot=<scratch>/math.png --window-size=1000,1400 file://...`, and look at the screenshot (Read tool). Scratch: /private/tmp/claude-501/-Users-jasonlai150-Documents-GitHub-4440/b8ce7be5-28b5-4a05-868b-6c0cf5495ca3/scratchpad/
